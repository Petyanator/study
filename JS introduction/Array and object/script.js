// array.push - puts one more element to the array from the end
// array.unshift - puts one more element to the array from the start
// array.shift - used to remove first element 
// array.pop - used to remove last element( or use array.splice(indexToRemove, 1))
// split ('/') - will find / in the object
// - **`concat(array2, array3, ...)`**: Merges two or more arrays into a new array without modifying the original arrays.
// sort(compareFunction): Sorts the elements of an array in place according to a specified comparison function, or alphabetically if no function is provided.
// reverse()`**: Reverses the elements of an array in place, changing the array’s order.
// map(...Some Function...)`: Creates a new array with the results of calling a provided function on every element in the original array.
// indexOf(value)`**: Returns the first index at which a given element can be found in the array, or `-1` if it is not present.
//Class to create the users
// class Person
// {
//     // Constructor to initialize user properties
//     constructor(name, age, dob) 
//     {
//         this.name = name;
//         this.age = age;
//         this.dob = dob;
//     }
//    // Function for THIS CLASS ONLY
// }